## Bezug

<!-- Ticket-Nummer aus AUFGABEN.md, z. B. "Ticket 1.5" — oder "kein Ticket". -->

## Was & warum

<!-- Kurz: was ändert sich, warum. -->

## Checkliste

- [ ] Ticket-Akzeptanzkriterien geprüft (bzw. n/a)
- [ ] `npm run typecheck && npm run lint && npm run format:check && npm run test:coverage && npm run build` lokal grün
- [ ] CI grün
- [ ] Goldene Regel (Sim-Grenze) eingehalten
- [ ] Keine offenen `TODO(Rückfrage)` ohne Notiz in `src/ARCHITEKTUR.md`
- [ ] Kein Scope-Creep über das Ticket hinaus
